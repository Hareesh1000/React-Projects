const menuItems = ['All Items', 'Fresh Vegetables','Herbs & seasonings','Dry Fruits','Organic','Seasonal picks'];


const items = menuItems.map(
    (el)=>{
        return el
    }
)


console.log(items)